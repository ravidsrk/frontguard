node_modules
dist
.frontguard-debug
*.tsbuildinfo
auth.json
.env
.env.*
# ...but the checked-in template must be committable. The broad `.env.*` above
# also swallowed `.env.example`, which is why the repo had no environment
# reference at all: every attempt to add one was silently ignored.
!.env.example
.dev.vars
.dev.vars.*
*.log
# Core dumps — match the dump files at repo/package/app roots only, never the
# `src/core/` or `test/core/` source directories (the old bare `core` pattern
# silently excluded real source files).
/core
/packages/*/core
/apps/*/core
/integrations/*/core
core.*
.frontguard/
frontguard-report/

# Deployment-specific

# Next.js
.next/

# Test coverage
coverage/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build/Deploy
.turbo/
.vercel/
.netlify/

# Release artifacts (scripts/release.sh)
.release-notes/
*.tgz

# build artifacts under apps/demo
apps/demo/next-env.d.ts
apps/demo/out/
apps/demo/tsconfig.json

# Mogra workspace metadata
.mogra/
.mogra

# Internal adversarial-audit workspace — quotes live secrets, never commit
.frontguard-audit/
