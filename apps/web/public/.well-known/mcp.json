{
  "name": "@frontguard/mcp",
  "description": "Pre-release Frontguard MCP server for querying a verified cloud API deployment. No default hosted endpoint is live.",
  "version": "0.2.3",
  "documentation": "https://frontguard.dev/agents.md",
  "package": {
    "registry": "npm",
    "name": "@frontguard/mcp"
  },
  "api": {
    "baseUrl": "https://your-frontguard-api.example.com"
  },
  "transport": {
    "type": "stdio"
  },
  "run": {
    "command": "npx",
    "args": ["-y", "@frontguard/mcp"],
    "env": {
      "FRONTGUARD_API_URL": "https://your-frontguard-api.example.com",
      "FRONTGUARD_API_KEY": "fg_live_xxx"
    }
  },
  "env": [
    {
      "name": "FRONTGUARD_API_URL",
      "required": true,
      "description": "Verified Frontguard API deployment URL. No default hosted endpoint is live."
    },
    {
      "name": "FRONTGUARD_API_KEY",
      "required": true,
      "description": "Frontguard API key.",
      "example": "fg_live_xxx"
    }
  ],
  "tools": [
    {
      "name": "list_regressions",
      "description": "Return the visual regressions Frontguard detected on a given GitHub PR (or run id). Each row carries a `diffId` you can pass to `get_suggested_fix`."
    },
    {
      "name": "get_suggested_fix",
      "description": "Return the AI-generated patch for a single diff (keyed by the `diffId` from `list_regressions`)."
    },
    {
      "name": "accept_baseline",
      "description": "Record run-scoped whole-run approval after `confirm_all_regressions_reviewed: true`. Screenshot promotion is not implemented yet; use the CLI or composite Action to update baselines."
    },
    {
      "name": "recent_runs",
      "description": "List the most recent runs the API key has access to. Optional `repo` and `branch` filters narrow the results."
    }
  ]
}
